# Conversational AI Assistant (RAG-based)

A chatbot using OpenAI + FAISS.

## Features
- Vector search
- FastAPI backend
- Simple frontend

## Run
pip install -r requirements.txt  
uvicorn app.main:app --reload
